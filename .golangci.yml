version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  settings:
    govet:
      enable-all: true

issues:
  exclusions:
    rules:
      # Test files have relaxed rules
      - path: "_test\\.go"
        linters:
          - errcheck
